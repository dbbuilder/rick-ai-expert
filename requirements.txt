# Core AI - Rick only needs OpenAI
openai>=1.0.0

# Web framework and API
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=11.0.0
jinja2>=3.1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
loguru>=0.7.0
python-multipart>=0.0.6

# Database - sqlite3 is built into Python
# RAG system uses simple TF-IDF, no heavy ML dependencies needed